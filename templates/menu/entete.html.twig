<div class="entete">
    <h1>Bucket-List<a href="{{ path('main_home') }}"><img src="{{ asset('img/logo.svg') }}"></a></h1>

    <nav class="manav">
        <ul>
            {% if app.user %}
            <li> <div class = "bonjour">Bonjour {{ app.user.username }} !</div></li>
            {% endif %}
            <li><a href="{{ path('main_home') }}">Home</a></li>
            <li><a href="{{ path('wish_list') }}">Wish list !</a></li>
            <li><a href="{{ path("main_aboutus") }}">About us !</a></li>
            {% if app.user %}
                <li><a href="{{ path('wish_add') }}">Add a wish !</a></li>
                <li><a href="{{ path('app_logout') }}">Logout</a></li>

            {% else %}
                <li><a href="{{ path('app_login') }}">Login</a></li>
                <li><a href="{{ path('app_register') }}">Register</a></li>
            {% endif %}

        </ul>
    </nav>
</div>